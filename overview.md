# 任务完成概览：每日≥500条采集量与80-100条涉华数据指标

## 已完成功能

### 1. 后端采集强化
- **采集周期**：`server/server.js` 将轮询间隔从 90 秒压缩到 **30 秒**，每轮抓取媒体源 60 重点 + 40 轮询、智库 20 重点 + 25 轮询。
- **涉华专项采集**：新增 `_runChinaFocus`，调用 `globalmedia.scrapeChinaFocus` 对高命中中文媒体、港台媒体、涉华外媒/智库进行专项抓取。
- **日指标统计**：新增 `_dailyStats`、`_isChinaLinked`、`/api/media/daily-stats` 端点，按 URL 去重后写入 PostgreSQL，并实时返回：
  - `runtime`（本次启动累计）
  - `database`（当日数据库真实落盘）
  - `targets`（总量≥500、涉华80-100）
  - `gaps`（距离目标缺口）

### 2. 数据源与闸门优化
- `server/media_feeds.js`：新增 `CHINA_FOCUS_SOURCES` 涉华专项源，补充中国官方/港台/涉华国际媒体与智库。
- `server/globalmedia.js`：新增 `scrapeChinaFocus`，对 Chinese source 含中国关键词且非文化娱乐噪声的条目额外放行。
- `gate.js` / `server/scrapers.js`：扩展涉华关键词，提升 A/B 维度命中。
- 修复 `server/media_feeds.js` 中 El País RSS URL 格式错误、Xinhua World HTML 页面误作 RSS 的问题。

### 3. 前端实时指标展示
- `app.js`：首页「实时情报流」面板标题直接显示今日采集进度：
  - `📊 <总量>/<500>`
  - `🇨🇳 <涉华>/80`
- 每 30 秒轮询 `/api/media/daily-stats` 刷新指标。

### 4. 验证结果
- 后端所有修改文件通过 `node --check`。
- 后端服务已重启，`/api/health` 正常。
- 实测约 20 分钟运行后：
  - 数据库当日 `osint_intel`：**299 条**
  - 数据库当日涉华：**100 条**（已达标 80-100 指标）
  - 总量缺口：**201 条**（按当前速率可在数小时内达标）

## 需要用户操作
- **Ctrl + Shift + R 硬刷新浏览器**，加载 `app.js?v=115` 新版本后可在首页实时情报流标题看到今日采集指标。
- 后端服务已自动启动并在后台运行，无需手动干预。

---

# 2026-08-25 批次：时效铁律收紧 + 翻译要素补全 + 涉华采集提质增量

## 一、Kabul Now 数据源核查
已在库且健康：`server/media_feeds.js` 阿富汗组 `https://kabulnow.com/feed/`（全站 RSS，覆盖用户点名的 /category/news/afghanistan/ 频道）。实测 HTTP 200、12 条、最新条目 2026-08-24。

## 二、百度翻译「标准版」验证 + 熔断自愈
- 用户切换标准版后直连 API 实测恢复正常（54004 余额耗尽错误消失）。
- 发现隐患：进程内存熔断会锁到次日 0:05，账户恢复后当天仍不用百度。
- 修复：熔断 30 分钟自愈探针——试探单成功即解除熔断，通道自动恢复。

## 三、24 小时时效铁律收紧
- 实测近 3 天 576 条入库：**43% 无任何日期字段**，旧闸"解析不出日期→放行"是旧闻漏网主通道。
- 新闸三途径：正文事件时间提取 → metadata 发布时间 → **URL 路径日期提取**（/2026/08/23/）；三途径全无日期的新闻**一律拦截**。
- 手工建案（无 URL 无日期）豁免，默认当下时间。

## 四、数据要素补全
- 实测近 3 天 581 条：**factSheet 结构化要素缺失率 100%**、event_date 缺失 94%。
- 修复：在全部 10 个入库路径的共同咽喉 `_tagAssets` 挂要素抽取（纯正则零网络开销）——伤亡/威胁行为体/事件性质/中方主体/金额/时间线索/处置行动，全部附原句佐证；event_date 三源回填。

## 五、涉华采集提质增量（含重大根因修复）
- **云采集管道空转根因**：Neon 表 int8 主键经 pg 驱动返回字符串，游标字符串比较 `"100">"99"=false` → 游标永久卡死，每 10 分钟重复拉同一批 194 条全部撞重复、入库 0。修复数值化后游标即刻 99→422，云端积压清零。
- **涉华负面 8 条新主题**（本地+云端每小时必查）：涉华项目抗议、债务陷阱叙事、中资项目受阻、排华/针对华人事件、间谍渗透指控、科技封堵国安审查、涉疆叙事、南海台海摩擦。
- **AP 无日期救回**：云端 AP 主题条目 1/3 无日期且 URL 无内嵌日期，入库闸前限量回抓文章页发布时间（首轮救回 4/12），其余交时效闸如实拦截。

## 六、代码变更与验证
| Commit | 内容 |
|---|---|
| e9bdb41 | 时效铁律收紧（无日期拦截+URL日期兜底）+ 百度熔断自愈 |
| e2b48cf | factSheet 全路径要素抽取 + event_date 三源回填 |
| b8b7ba5 | Neon 游标根因修复 + 涉华负面 8 主题 + AP 无日期回抓 |

- 三文件 `node --check` 通过；PM2 重启后 health 200；要素抽取冒烟测试通过。
- 重启后首轮云同步：游标 99→422、积压 194 条全部过闸处理、AP 日期救回 4/12、翻译 41/41 成功。
- 已推送 main，云端 GitHub Actions 下轮自动生效涉华负面新主题。
